# Supabase

Use Supabase for database, auth, storage, and RLS when production credentials are available.

The frontend currently uses local seed data. The domain model is shaped for migration into Supabase tables documented in `docs/DATABASE.md`.
