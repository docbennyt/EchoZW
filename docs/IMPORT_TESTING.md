# Import Testing

Added coverage:

- CSV parsing, required-field validation, formula-safe notes, venue normalization, typo warnings, and invalid times.
- Structured DOCX row normalization with selected cohort context.
- Assisted master PDF extracted-text parsing with first-draft, slashed-course, break, and lunch warnings.
- Cohort grouping, cohort summaries, overlap detection, stable session keys, and draft readiness checks.
- Migration contract checks for normalized tables, private storage, RLS, app-metadata authorization, verification state, and traceability columns.

Live Supabase migration execution was not performed in this session because MCP authentication was unavailable to the shell environment.
